# Archivo de sesiones — 2026-05-27 → 2026-06-08

> Sesiones cerradas y mergeadas a `main`. Conservado para arqueología; no
> se lee al inicio. El estado vivo está en `../project-status.md`.
>
> Cubre: endurecimiento de producción (audit 2026-06-08, Fases 0/1 +
> Bloques 1/2), F15-5, TT-8 root, y los audits arquitectónicos 2026-05-27
> (macro + per-package) ya cerrados (NN/PP/QQ/OO/MM/RR, SS-1..6, TT-5/6/7).

---

## 🌐 2026-06-08 (parte 3) — Endurecimiento prod: Fase 0 (seguridad) + Fase 1 (despliegue) + Bloques 1/2

Rama `claude/project-review-PO25J` (PRs #499 + #500, mergeadas). A partir
del audit `audit-2026-06-08-production-readiness.md`. Enfoque
**plug-and-play** (estilo Plex): defaults seguros sin configurar nada,
cero pérdida de datos. Todo con tests; build/vet/`-race` verdes.

### Fase 0 — bloqueantes de exposición a internet
| Item | Fix |
|---|---|
| C1 | `extractToken` ya no acepta `?token=` (solo Bearer/cookie) — evita fuga del token por logs/Referer/historial. `middleware_test.go` |
| C2 | `logging.RedactURL` + redacción por-valor en el `ReplaceAttr` central → enmascara user/pass en URLs IPTV (m3u/epg/upstream). `logging_test.go` |
| A1 | `IPRateLimitMiddleware` en login/refresh/setup/device (`NewAuthRateLimiter` 30/min·burst 10 por IP) |
| A2 | Lockout de login por tupla `user:<u>@<ip>` (no username global) → mata el DoS de cuenta. `service_test.go` |
| A3 | Proxy IPTV firma (HMAC-SHA256) las URLs reescritas; el handler exige+verifica firma → cierra el relay HTTP abierto. **No** host-lock (compatible multi-CDN). `proxy_sign_test.go`, `iptv_test.go` |
| A4 | Quitadas las 4 cabeceras `Access-Control-Allow-Origin: *` del proxy HLS |
| A5 | `observability.metrics_token` (+env) gate Bearer/`?token=` en `/metrics` + aviso si expuesto. `metrics_auth_test.go` |

### Fase 1 — robustez de despliegue
| Item | Fix |
|---|---|
| A7 | `tini` PID 1 en ambos stages del Dockerfile (reapea ffmpeg huérfanos + SIGTERM) + `init:true` en compose |
| A8 | `stop_grace_period: 40s` en los compose (la app drena en 30s) |
| M10 | `SaveDatabaseConfig` ancla el SQLite vacío/relativo bajo `/config` → no pérdida de datos al recrear contenedor. `service_test.go` |
| A6 | nginx: bloque SSE dedicado (`proxy_buffering off`) → progreso de scans/uploads/pairing en tiempo real |
| M9 | nginx: `location /` body acotado a 1g; subidas tus por bloque `/api/v1/uploads/` propio (sin tope) |
| A5-perímetro | `deny`/`403` de `/metrics` en nginx y Caddy |
| M11 | Backup SQLite automático (`VACUUM INTO`) antes de migrar, keep 3, best-effort. `build_database_backup_test.go` + integración real |

**Diferidos a propósito** (forzarlos rompería plug-and-play): **M5**
(password Postgres — opt-in/avanzado, SQLite es default), **M7** (límites
mem/cpu — dependen del hardware). **Hallazgo afinado:** el supuesto bug
de DB efímera (M10) era parcial — la imagen ya pasa `--config
/config/hubplay.yaml`; aun así se añadió la defensa.

### Bloque 1 — seguridad media
| Item | Fix |
|---|---|
| M4 | `trustForwardedProto` borra `X-Forwarded-Proto` de peers no declarados en `trusted_proxies`. `forwarded_proto_test.go` |
| M2 | `EnforcePasswordChange` bloquea mutaciones (403) de usuarios con `password_change_required` salvo `/me/password` + `/auth/logout`; mira el flag en DB. `password_change_test.go` |
| M8 | `Permissions-Policy` en la app (deniega camera/mic/geo/payment/usb; no toca fullscreen/autoplay/PiP). `security_headers_test.go` |
| M3 | `RequirePrivateClient`: `/auth/setup` y `/setup/*` (salvo `/setup/status`) solo desde IP privada/loopback. `setup_network_test.go` |
| B5 | `ReadHeaderTimeout: 10s` en el `http.Server` (slowloris) |
| M1 | Verificado: el double-submit CSRF ya exige header en mutaciones cookie-auth. Sin cambio. |

### Bloque 2 — frontend hardening
| Item | Resultado |
|---|---|
| A11 | Error boundary por ruta en `AppLayout` (`key={pathname}`): un crash de página no tira el shell. `ErrorBoundary.test.tsx`. |
| A12 | `MediaGrid` virtualizado (window scroll, `@tanstack/react-virtual`). DOM acotado: pico ~72 tarjetas / 5000 (vs 5000), verificado en Chromium real (`web/verify/`). Patrón EPGGrid: umbral `VIRTUALIZE_THRESHOLD=60` (pequeños = ruta directa testeable; grandes = subcomponente `VirtualizedMediaGrid`), una sola fuente de columnas (`columnsForWidth`+`useGridColumns`). Directivo `"use no memo"` confinado al subcomponente (el compiler cachea el store del virtualizador; `incompatible-library` no reconoce `useWindowVirtualizer`). Override de eslint scopeado por desfase de rc2. Stub `ResizeObserver` en setup. |
| M22 | **No-issue**: `JWTSecret` auto-gen solo siembra el keystore en el 1er arranque; luego las claves viven en DB (persistidas). Réplicas con DB compartida comparten clave → ya mitigado. |

**Verificación en navegador** (`web/verify/`): arnés standalone +
Playwright (`measure-grid.mjs`, `PW_CHROME`) que mide tarjetas en DOM y
reciclado. No entra en build ni CI (manual). `playwright-core` devDep.

---

## 🌐 2026-06-08 (parte 2) — F15-5 integration tests library

Cerrado el último item de severidad media del audit 2026-05-14. Nuevo
`internal/api/handlers/library/library_integration_test.go` (~9 tests):
cabla el `library.Service` real sobre repos sqlc reales
(`testutil.NewTestRepos`) y dirige requests HTTP por chi, ejercitando
**Handler → Service → Repository → DB**. Cubre round-trip de persistencia
+ `item_count`, 404 sentinel, ACL `ListForUser` (INNER JOIN
`library_access`), paginación keyset, cap por content-rating en SQL
(`IN (...)`, NULL excluido), orden `added_at DESC`, genres desde
`item_value_map`. Libs en `ScanMode: "manual"` para no disparar
auto-scan; `svc.Shutdown` en `t.Cleanup` (LIFO). Dual-backend. 0 cambios
de producción.

## 🌐 2026-06-08 — TT-8 root + limpieza docs

Traducidos a español los 9 ficheros compartidos del paquete raíz
`internal/api/handlers/` (`interfaces.go`, `responses.go`, `contracts.go`,
`item_helpers.go`, `sse_limiter.go`, `streaming_deadline.go`; el resto ya
estaban). Sólo comentarios, 0 lógica. Sub-paquetes (`admin`, `auth`,
`federation`, `iptv`, `me`, `media`, `system`) quedan pendientes
incrementalmente.

---

## 🧹 2026-05-30 — TT-5 + TT-7 + SS-2 + auditoría fresca

Rama `claude/project-review-mnJ9a`. Cerrados los 3 olores satélite
accionables del audit per-package.

- **TT-5** — `apperror.recorder` `func` plano → `atomic.Pointer[func]`
  (data race latente bajo `t.Parallel`). `init()` instala el no-op.
- **TT-7** — `NewItemHandler` 15 params posicionales → `media.ItemHandlerDeps`
  (precedente `stream.Deps`, `iptv.Deps`).
- **SS-2** — `upload`/`audit` dejan de importar `db`: cada uno define su
  struct espejo (`upload.AuditRow`, `audit.LogRow`); conversión a `db.*`
  en 2 adapters en el composition root (`cmd/hubplay/audit_adapters.go`).
  Patrón: **adapter en la frontera**.
- **Auditoría fresca** del backend sin hallazgos accionables nuevos. El
  supuesto deadlock en `stream/manager.go` `RestartSessionAt` se verificó
  **falso positivo** (m.mu se libera antes de tomar restartMu; sin ciclo).

## 🏛 2026-05-28 (continuación) — OO + MM + RR cerrados

Rama `claude/project-review-97WBR`.

- **OO** — Split del mega-paquete `handlers/` (60+ ficheros, 15 dominios)
  en 10 sub-paquetes por dominio (`system`, `authhandler`, `admin`,
  `users`, `media`, `me`, `fedhandler`, `iptvhandler`, `libhandler`,
  `uploads`) + root compartido. Helpers de respuesta exportados; cada
  sub-package define micro-interfaces locales. 126 ficheros, +3187/-2293,
  0 regresiones de API HTTP.
- **MM** — `api.Dependencies` (71 campos planos) → 11 sub-structs por
  dominio (`Infra`, `Server`, `Auth`, `Catalog`, `Streaming`, `IPTV`,
  `Federation`, `Providers`, `Admin`, `Setup`, `Uploads`) en
  `internal/api/deps.go`. `router.go` 581→362 LoC. Accesos via
  `deps.Group.Field`.
- **RR** — Eliminado `handlers/deps_repos.go` (19 interfaces broad sólo
  usadas por `Dependencies`); movidas a `internal/api/repos.go`.

## 🏛 2026-05-28 — Audit arquitectónico profundo + implementación

Rama `claude/go-backend-arch-review-UDzqB`, PR #477. 21 commits, 28
paquetes verdes. Olores cerrados: **NN** (interfaces gigantes → 20+
micro-interfaces, `interfaces.go` 449→279), **TT-6** (IPTVHandler god →
facade + 9 sub-handlers), **SS-3/4/5** (defer fuera de lifecycle, os.Exit,
browse sin timeout), **QQ** (main.run 780→581 LoC, 5 builders), **PP**
(tipos db fugan: 35→15 imports, 14 tipos → `library/model` +
`provider/model`). Análisis en `archive/audit-2026-05-27-per-package-review.md`.

## 🏛 2026-05-27 (parte 5) — Audit arquitectónico macro

Documento `archive/audit-2026-05-27-architecture-macro.md`. Catalogó los
olores estructurales MM/NN/OO/PP/QQ/RR (de shape, no bugs) que emergieron
por agregación tras cerrar lo táctico del audit 2026-05-14. Todos cerrados
después (ver sesiones 2026-05-28).

## 🧪 2026-05-27 (parte 4) — F15-6 error coverage

PR #471. 6 tests en `library_test.go` para error opaco (5xx no-AppError):
si el repo devuelve un error genérico, el handler debe rendir 500 vía
`handleServiceError` default, no 200 con lista vacía.

## 🧹 2026-05-27 (parte 3) — LL Transcoder stateless + SignPath + Dependabot

- **LL** (#468) — `Transcoder` stateless: eliminado `sessions map` + `mu`
  + 4 métodos de tracking. El tracking vive solo en `Manager.sessions`
  (unicidad ya garantizada por `singleflight`). -130 LoC. Último olor alto
  del audit 2026-05-14 (6/6).
- **SignPath** (#469) — firma Authenticode opt-in del installer Windows,
  gated tras `vars.HUBPLAY_SIGNING_ENABLED == 'true'`. Guía en
  `docs/architecture/windows-installer-signing.md`. **Pendiente operador**:
  aplicar en signpath.org, configurar dashboard, activar la var.
- **F15-3/4/9** cerrados por análisis (ya resueltos por F15-1 / patrones
  legítimos / Go 1.23+ runtime cleanup).

## 🎬 2026-05-27 (parte 2) — VideoPlayer 3ª ola + F15-2 (11 PRs)

VideoPlayer 787→652 LoC (-17%) extrayendo hooks (`useVideoElementSync`,
`useStreamSessionCleanup`, `useStartPositionSeek`, `useFullscreenSync`,
`useExternalSubMode`, `usePlayerActions`). **F15-2** (#466): clock seam
para los 21 db repos via **package-level seam** (`var timeNow = time.Now`
en `internal/db/now.go` + helper `SetTimeNowForTest`) — idiomático cuando
la API es ancha (33 callsites) y la DI desborda el beneficio.

---

## 🗂 Audit 2026-05-14 — quick reference (todo cerrado ✅)

Archivo completo: `archive/audit-2026-05-14-go-backend-review.md` +
tracker `archive/intervention-2026-05-14.md`.

### Olores altos (6/6 ✅)
| Olor | Cerrado por |
|---|---|
| A+M (`*db.X` en services) | Sesión 2026-05-21 (H deps-interfaces #419) |
| B+J (ciclos observability ↔ stream/handlers) | Sink pattern |
| CC (iptv.Service god-struct) | Split CC fase 1+2 |
| P (ItemHandler god 1186 LoC) | Split en 5 sub-handlers via facade |
| W (router.go 1549 LoC) | Split en 7 mount_*.go |
| F14-2-a (BuildFFmpegArgs 13 params) | Struct `TranscodeRequest` |
| G (composition root) | `lifecycle.go` + `library.Module` + `iptv.Module` |
| H (`*db.X` en `Dependencies`) | Interfaces broad (#419) |
| LL (doble session tracking) | Transcoder stateless (#468) |

### Olores medios (todos ✅)
F14-3/4/5/6/7-a/9/9-a/10-a/12-a, F15-1 (41 sleeps eliminados), F15-2
(clock seam), F15-3/4 (ya cerrados por F15-1), F15-5 (integration tests
2026-06-08), F15-6 (#471), F15-7 (+61 t.Parallel), F15-8 (`t.TempDir()`),
F15-9 (análisis), F16 (8/8 medium + 10/10 bajas).

### Olores bajos (polish, abiertos opcionales)
- **F15-10/11/12** — fakes compartidos, naming, concurrency tests.
