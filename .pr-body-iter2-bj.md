## Resumen

Iteración 2 completa de la intervención post-auditoría 2026-05-14. Cierra los **5 olores estructurales** del bloque "sub-paquetes de db/" (B, J, K, T, L) en 3 commits temáticos.

| Commit | Olores | Qué hace |
|---|---|---|
| `dc988ba` | **B + J** | Mueve `internal/db/federation_repository.go` (1 474 LOC, 6+ responsabilidades) a `internal/federation/storage/` con split en 9 ficheros temáticos. Cierra la **única violación de capa real** del proyecto (`db → federation`). |
| `ab0f4c1` | **L** | Split textual de `internal/db/home_repository.go` (671 LOC) en 4 ficheros por carril: `home.go` (struct + constructor + helpers), `home_trending.go`, `home_recommended.go`, `home_live.go`. |
| `13fcff6` | **K + T** | Crea `db.ActivityRepository` + 3 interfaces estrechas (`HealthChecker`, `BackupOperator`, `PoolStatsReporter`) + struct `Maintenance`. **Elimina `Dependencies.Database *sql.DB`** por completo. |

Cero cambios de API HTTP pública. Sólo cambian nombres de paquetes + tipos internos.

## Cambios estructurales

### Commit 1: `fix(federation): B + J` — `internal/federation/storage/`

| Antes | Después |
|---|---|
| `internal/db/federation_repository.go` (1 474 LOC) | eliminado |
| `internal/db/federation_repository_test.go` (498 LOC) | renombrado a `internal/federation/storage/repository_test.go` (`package storage_test`) |
| — | `internal/federation/storage/storage.go` (struct + constructor + dialect helpers) |
| — | `internal/federation/storage/sql_util.go` (helpers privados) |
| — | `internal/federation/storage/identity.go` / `invite.go` / `peer.go` / `audit.go` / `share.go` / `item_cache.go` / `progress.go` |

Tipo `Repository` (no `FederationRepository`) para evitar stutter. Construcción se queda en el composition root (`main.go`, `pg-smoke`, tests). El paquete `federation` consume el repo vía `type Repo interface` (manager.go:34) — cero ciclo `federation ↔ federation/storage`.

Callers actualizados: `cmd/hubplay/main.go:392`, `cmd/pg-smoke/main.go:107`, `internal/api/handlers/federation_stream_test.go:73,454`.

### Commit 2: `refactor(db): L` — split de `home_repository.go`

Split textual puro (cero cambios de SQL, tipos públicos o comportamiento):

| Fichero | Contenido |
|---|---|
| `internal/db/home.go` | struct `HomeRepository` + `NewHomeRepository` (7 queries SQL pre-rewritten) + `groupConcatExpr` helper + `splitGroupConcat` helper (compartido) + `IDsFromTrending` utility |
| `internal/db/home_trending.go` | `HomeTrendingItem` type + `Trending` method |
| `internal/db/home_recommended.go` | `HomeRecommendation` + `HomeBecauseSeed` + `HomeBecauseResult` types + `Recommended` + `BecauseYouWatched` methods |
| `internal/db/home_live.go` | `HomeLiveNowChannel` type + `LiveNow` method |

El plan del audit mencionaba `home_latest.go`, pero no existe método `Latest` en el repo — el split refleja la realidad. Recommended y BecauseYouWatched comparten `splitGroupConcat` y la estrategia de scoring por género; viven juntos.

### Commit 3: `refactor(db): K + T` — interfaces estrechas + ActivityRepository

**Nuevos ficheros en `internal/db/`**:

- `activity_repository.go` — `ActivityRepository` con `DailyWatchActivity` + `TopItems`. Tipos `DailyWatchBucket` y `TopItemRow` exportados. Las queries raw que estaban en `handlers/system.go:430-540` y `system.go:570-625` ahora viven aquí, pre-rewritten una vez al construir.
- `maintenance.go` — 3 interfaces estrechas (`HealthChecker { PingContext }`, `BackupOperator { VacuumInto }`, `PoolStatsReporter { Stats }`) + struct `Maintenance` que las implementa. `MigrationSource()` retorna el `*sql.DB` subyacente exclusivamente para el migrator sqlite→pg (único caller legítimo).

**Handlers adaptados (4 sitios)**:

| Handler | Antes | Después |
|---|---|---|
| `handlers/system.go` | `db *sql.DB` + `driver string` + queries inline | `health db.HealthChecker` + `activity *db.ActivityRepository`; thin wrappers de las queries del repo |
| `handlers/health.go` | `db *sql.DB`, `db.Ping()` | `health db.HealthChecker`, `health.PingContext(r.Context())` |
| `handlers/admin_backup.go` | `db *sql.DB`, `Exec("VACUUM INTO ...")` | `backup db.BackupOperator`, `backup.VacuumInto(ctx, path)` |
| `handlers/admin_db.go` | `liveDB *sql.DB` | `maint *db.Maintenance`; `Stats()` y `MigrationSource()` |

**`api.Dependencies`**:
- **Eliminado** `Database *sql.DB`.
- **Añadido** `DB *db.Maintenance` + `Activity *db.ActivityRepository`.
- `internal/api/router.go` ya no importa `database/sql`.

**`cmd/hubplay/main.go`**: añade `db.NewMaintenance(driver, database)` + `db.NewActivityRepository(driver, database)` al `Dependencies` literal.

**Tests ajustados**: 10 sitios en `system_test.go`, 6 en `health_test.go`, 2 tests de integración (`integration_test.go` + `stream_integration_test.go`).

## Test plan

- [x] `go build ./...` — exitcode 0 en container `golang:1.25`.
- [x] `go test ./internal/... -count=1 -timeout=300s` — **22 paquetes verde**:
  - `hubplay/internal/api` (9.9 s)
  - `hubplay/internal/api/handlers` (17.2 s)
  - `hubplay/internal/auth` (14.1 s)
  - `hubplay/internal/db` (26.7 s, sin regresión tras la extracción)
  - `hubplay/internal/federation` (2.4 s)
  - `hubplay/internal/federation/storage` (3.0 s, nuevo paquete; las 4 funciones test movidas pasan)
  - … el resto del repo verde.
- [x] Tests pre-existentes preservados: `TestFederationRepository_*` (4), `TestHealthHandler_*` (6), `TestSystem_*` (10), `TestAdminDB_*`, `TestAdminBackup_*`, 2 tests de integración.

## go.mod

`jackc/pgx/v5` y `jackc/pgerrcode` promovidos de `// indirect` a direct (cambio automático de `go build` en el commit de B+J; el código los usa transitivamente igual que antes).

## Pendiente (siguiente iteración)

Iteración 3 del plan (`docs/memory/intervention-2026-05-14.md`): migración Opción B incremental de tipos de feature `internal/db/` → cada feature (`iptv`, `auth`, `library`) define sus propios tipos. ~3-4 días estimados.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
