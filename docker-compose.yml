services:
  hubplay:
    build: .
    container_name: hubplay
    ports:
      - "8096:8096"
    volumes:
      - ./config:/config
      - ./cache:/cache
      # Map your media folders here:
      - /mnt/media:/mnt/media:ro
    environment:
      - TZ=Europe/Madrid
    restart: unless-stopped
